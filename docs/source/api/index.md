# API Reference

```{note}
API documentation will be auto-generated from the Python bindings
(`chemgp-py`) using sphinx-autoapi once the bindings are expanded.
For Rust API documentation, run `cargo doc --open -p chemgp-core`.
```

## Rust API

Generate and view the Rust documentation:

```shell
cargo doc --open -p chemgp-core
```

## Python API

The Python bindings are currently minimal. As they are expanded, this
section will include auto-generated documentation for all exposed
classes and functions.
